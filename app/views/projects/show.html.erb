  <section class="container">
        <hr>
        <h5 class='title' style="text-transform: uppercase;"><%= @project.name %></h5>
        <hr>

        <!-- --><center>
        <div class="card" style="width: 70%;" >
            <div class="card-body">
                <h6 class="card-title space">
                <label> <%= @project.creator.username %> | <%= @project.name%></label>
                <%= link_to edit_project_path(@project) do %>
                    <%= image_tag 'https://img.icons8.com/windows/32/000000/settings--v1.png', alt: @project.name %>
                <% end %>
                </h6>
                <hr>
                <h6 class="card-subtitle mb-2 text-muted"> Description </h6>
                <p class="card-text"> <%= @project.description %> </p>
                <hr>
                </form>
                <div style="width: 43rem; text-align: left;">
                    <label style="text-align: left; color: #4dccbd">All Members</label><br>
                    <% @project.project_users.each do |project_user| %><br>
                    <% user = project_user.user %>
                    <div class="update" style="text-align: left">
                        <p><%= user.username %> &nbsp; &nbsp; <%= user.email %>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; <label style="text-align: left;"><%= project_user.role %></label></p>
                    </div>
                    <% end %>
                </div><br>
                <div class="buttom space">
                    <label>
                        <img width="20px" src="https://img.icons8.com/ios-filled/50/000000/group-foreground-selected.png"/>
                        <span>1</span>
                    </label>
                      <%= button_to image_tag("https://img.icons8.com/wired/64/000000/filled-trash.png", width: "20px"), delete_project_path(@project), method: :delete, class: "btn btn-danger" %>

                </div>
            </div>
        </div>
        </center>

    </section>
